{
  "name": "idle-human",
  "version": "1.0.0",
  "description": "Bir insandan başlayıp medeniyeti büyüten idle/clicker oyunu",
  "private": true,
  "scripts": {
    "serve": "npx --yes http-server www -p 8000 -c-1 -o",
    "serve:py": "python3 -m http.server 8000 --directory www",
    "serve:py-win": "py -m http.server 8000 --directory www",
    "cap:sync": "cap sync android",
    "cap:copy": "cap copy android",
    "cap:open": "cap open android",
    "android:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "android:apk": "cap sync android && cd android && ./gradlew assembleRelease",
    "android:aab": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.5"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.5",
    "@capacitor/core": "^7.6.5"
  }
}
